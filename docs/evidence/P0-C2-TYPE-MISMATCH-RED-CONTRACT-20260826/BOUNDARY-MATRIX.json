{
  "task": "P0-C2-TYPE-MISMATCH-RED-CONTRACT-20260826",
  "case": "C2",
  "expected_contract": "reject authoritative mismatch before persistence/admission/spend",
  "current_state": "RED",
  "boundaries": [
    { "id": "E1", "path": "app/api/products/route.ts", "category_source": "request or default", "unchecked_effect": "directory preparation, temporary-file writes/cleanup, image storage, product persistence, and audit" },
    { "id": "E3", "path": "app/api/products/[id]/route.ts", "category_source": "request or existing row", "unchecked_effect": "product update" },
    { "id": "E6", "path": "app/api/dashboard/campaign/product/route.ts", "category_source": "URL keyword guess, request, or default", "unchecked_effect": "organization product persistence" },
    { "id": "E7", "path": "app/api/dashboard/campaign/product/route.ts", "category_source": "request or existing row", "unchecked_effect": "organization product update" },
    { "id": "A1", "path": "app/api/jobs/route.ts", "category_source": "stored product row", "unchecked_effect": "job snapshot, job insert, credit hold, enqueue" },
    { "id": "A2", "path": "app/api/dashboard/matrix/route.ts", "category_source": "stored organization product", "unchecked_effect": "matrix script/render delegation to A4" },
    { "id": "A3", "path": "app/api/dashboard/campaign/generate/route.ts", "category_source": "stored organization product", "unchecked_effect": "provider-consuming script generation and script persistence" },
    { "id": "A4", "path": "lib/dashboard/render-cell.ts", "category_source": "locked organization product row", "unchecked_effect": "job snapshot, job insert, credit hold, enqueue" }
  ],
  "proposed_seam": {
    "working_name_only": "validateAuthoritativeProductType",
    "production_implemented": false,
    "requires": ["declared opaque token", "independent trusted opaque token", "provenance"],
    "missing_policy_result": "UNDETERMINED_POLICY_INPUT",
    "reason_code_selected": false
  },
  "mutation_controls": {
    "accept_everything_killed_by": "trusted mismatch negative",
    "reject_everything_killed_by": "trusted match positive",
    "missing_signal": "undetermined, not fabricated"
  },
  "false_green_guards": {
    "parser": "TypeScript AST CallExpression per exported handler",
    "central_symbol_binding": "all handler imports plus app/lib production sources scanned; only builder and validator exports allowlisted; receiver, namespace, default, side-effect, relative, local shadow, split, aliased named export, indirect and literal/computed/const-bound dynamic access rejected",
    "central_export_surface": "only builder and validator runtime exports; default assignments, default-modified declarations, wildcard, re-export, class/enum and issuer aliases rejected",
    "comments_or_declarations_satisfy": false,
    "every_boundary_required": true,
    "effect_callback_ownership_required": true,
    "merely_prior_or_ignored_call_satisfies": false,
    "seam_rejection_must_be_awaited_or_returned": true,
    "required_opaque_inputs": ["typed declared source", "typed trusted provenance source"],
    "required_input_check": "buildAuthoritativeTypeBoundaryInput typed/dataflow boundary with opaque ingress-issued capability identity",
    "input_independence_mutations": ["same-expression alias", "same-data/different-ID structural forgery", "frozen clone of actually issued fields with valid prefix", "prefix-only acceptance", "frozen-only acceptance", "similarly named issued-capability positive"],
    "reference_probe_inputs": ["test-only issued trusted match", "test-only issued trusted mismatch", "missing policy"],
    "production_probe_inputs_before_ingress_authorization": ["frozen structural forgery", "missing policy"],
    "mismatch_contract": "throws/rejects, zero callback effects, cannot be ignored into success",
    "valid_contract": "exactly one callback effect",
    "missing_policy_contract": "UNDETERMINED_POLICY_INPUT and zero callback effects",
    "contract_test_issuer_location": "tests/fixtures only; absent from production module",
    "actual_handler_probe": "not required until an authoritative production-consumable signal ingress is approved",
    "sink_classes": ["storage", "product persistence", "persona persistence", "provider generation", "script persistence", "job persistence", "audit persistence", "credit hold", "managed trace queue", "ordinary queue"]
  }
}
