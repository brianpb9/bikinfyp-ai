import assert from "node:assert/strict";
import crypto from "node:crypto";
import fs from "node:fs";
const dir=new URL("./",import.meta.url);
const read=(name)=>JSON.parse(fs.readFileSync(new URL(name,dir),"utf8"));
const deploy=read("CAPTURE-DEPLOY.json"),job=read("CAPTURE-JOB.json"),health=read("CAPTURE-HEALTH.json"),proof=read("CAPTURE-PREFLIGHT.json");
const capture=read("capture-metadata.json");
for(const [name,digest] of Object.entries(capture.files)) assert.equal(crypto.createHash("sha256").update(fs.readFileSync(new URL(name,dir))).digest("hex"),digest,name);
const sha="885b7e03f75bd68ae4e53f7d74b99ab12304ceb6";
assert.equal(deploy.status,"live");assert.equal(deploy.commit.id,sha);assert.equal(health.build_sha,sha);
assert.equal(job.status,"succeeded");assert.equal(job.startCommand,"node scripts/staging-jj-glow-final-evidence.cjs preflight");
assert.equal(proof.event,"JJ_GLOW_METADATA_FREEZE_PASS");assert.equal(proof.deploySha,sha);
assert.equal(proof.jobId,"55284f20-efb8-4b18-8a24-f90fc91af733");assert.equal(proof.candidateCount,1);assert.equal(proof.scriptCount,1);
assert.equal(proof.referenceSha256,"744707593be97ac61673b03576e441bf1fd6793833830102cf2a2c9bdf8ae4c1");
assert.equal(proof.sourceR2Sha256,proof.referenceSha256);assert.equal(proof.snapshotR2Sha256,proof.referenceSha256);
assert.equal(proof.receiptR2Sha256,"ca3906a381e6d299bc46fe62aeefbc3bd9b4183a6ff59c4f3cde2ca8f94788c3");
assert.match(proof.referenceManifestSha256,/^[0-9a-f]{64}$/);assert.match(proof.productSnapshotSha256,/^[0-9a-f]{64}$/);
assert.match(proof.approvedScriptSha256,/^[0-9a-f]{64}$/);assert.match(proof.idempotencyKey,/^[0-9a-f]{64}$/);
for(const key of ["providerTasks","providerPosts","outputs","fypPosted","postPlans","terminalLedgerCount"]) assert.equal(proof[key],0,key);
assert.equal(proof.holdCount,1);assert.equal(proof.metadataMutation,false);assert.equal(proof.publication,false);assert.equal(proof.autoRetry,false);
console.log("JJ_GLOW_FINAL_METADATA_FREEZE_EVIDENCE=PASS");
console.log(`REVIEWED_RUNTIME_SHA=${sha}`);console.log(`READ_ONLY_JOB=${job.id}`);
console.log(`APPROVED_SCRIPT_SHA256=${proof.approvedScriptSha256}`);console.log("PROVIDER_POST_COUNT=0");console.log("EVIDENCE_LEDGER_COUNT=0");
