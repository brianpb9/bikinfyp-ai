# Candidate #4 activated-state pre-call gate and PostgreSQL typing remediation

Canonical task `1788216339000-reviewer-TASK` authorizes one existing Candidate #4 provider execution only after fresh metadata, digest, authorization, cost, lease, and effect gates. Raw managed jobs now prove the staging-worker key is present and active through a non-mutating authenticated `GET` returning `404`, and bind only its SHA-256 fingerprint. Complete paginated control-plane reads prove that fingerprint differs from production/shared. The same capture found and preserves a stale production/shared key on the old staging-web runtime; the exact key was removed from staging web only, production was untouched, exact runtime `23fa4923ec667a44ef8044e309140ee169864f88` was redeployed, and a new managed web job proves the key is absent at runtime.

The old pre-activation metadata command failed closed because it correctly found the already-created evidence row. The replacement worker-side receipt then passed a fresh `REPEATABLE READ READ ONLY` freeze at exact authorized provider runtime `4d1cf4fc375fbb75ed09de7f5ab36ce3f72b38a1`: PostgreSQL↔R2 source/snapshot/rights bytes, approved-script digest, lifecycle and cross-row identities, append-only runtime authorization, active lease, `1.134 <= 1.25` cost cap, paused queue, and all provider/output/publication counters at zero. It freezes Brand Fidelity, Anti-Slop, and Prompt/Verdict Archive as mandatory independent verdict lanes under contract digest `c5c8ac3c3dbc33548cef51ca6ac92ac4ed4ff744d805e19cd1c41ff25d4e6f5c`.

Two deterministic pre-POST failures are preserved. The first wrapper did not compile because top-level await was invalid in CJS. The corrected wrapper entered the worker, moved the job to resumable `GENERATING_VISUAL`, then PostgreSQL rejected the durable `0 -> 1` claim before any network call: one `$3` value was inferred simultaneously as legacy `TEXT` progress columns and `TIMESTAMPTZ` lease columns. A later activated-state freeze independently proves `provider_post_count=0`, no provider task/output/publication, the same R2/metadata/contract digests, and the paused queue.

The narrow fix casts the shared ISO timestamp separately at the SQL boundary. A real disposable PostgreSQL regression now proves an active lease can make exactly one durable claim across those mixed column types. The reviewed one-shot wrapper is exact worker/runtime/job bound, rejects deterministic mode, requires the staging key and R2/PostgreSQL identities, checks the queue is paused with zero active jobs, and requests no queue retry.

Remediation runtime `23fa4923ec667a44ef8044e309140ee169864f88` contains that fix plus an append-only successor authorization ledger. The successor insert is valid only when executed by that same exact deployed SHA and only while Candidate #4 remains `GENERATING_VISUAL`, `PREPOST_READY`, actively leased, and zero-effect. The fresh preflight and one-shot wrapper are both hard-bound to this corrected runtime; the original `4d1cf4fc375fbb75ed09de7f5ab36ce3f72b38a1` authorization remains immutable history.

Managed receipts bind both web and worker deployments to `23fa4923ec667a44ef8044e309140ee169864f88`. The append-only successor authorization succeeded under `SERIALIZABLE` with zero provider/task/output/publication effects. A subsequent worker-side `REPEATABLE READ READ ONLY` preflight re-read the successor row, live lease, all cross-row identities, PostgreSQL-to-R2 bytes, script digest, paused queue, and zero effects. The final wrapper independently hashes its runtime credential and rejects any fingerprint other than the managed active worker fingerprint before it can reach `processPostgresJob`.

The executable boundary is the committed external CJS artifact with SHA-256 `b7c500b1645dd737fe72c70a0b5dea7412045ad1d57d415fd43b57f5932a864e`. Its managed one-off `startCommand` embeds those exact bytes, independently hashes them before evaluation, and exposes two fail-closed modes over the same artifact. `ATTEST_NO_POST` loaded the real deployed worker modules, rechecked runtime/key/paused queue, and exited with no mutation or provider call. `EXECUTE_EXACTLY_ONCE` additionally requires the canonical task confirmation before the sole `processPostgresJob` call. The verifier decodes the raw managed `startCommand` and byte-compares it to the reviewed committed artifact.

No provider execution is authorized from this implementation packet until canonical Reviewer PASSes its exact SHA. The worker remains suspended, the queue remains paused, and the one provider POST is still unused.
