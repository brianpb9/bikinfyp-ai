{
  "id": "job-da88h8ifngtc73bn6iag",
  "status": "succeeded",
  "createdAt": "2026-08-27T18:57:38.972354Z",
  "startedAt": "2026-08-27T18:57:39Z",
  "finishedAt": "2026-08-27T18:58:06Z",
  "startCommand": "node --input-type=module -e \"import crypto from \\\"node:crypto\\\";import{Pool}from\\\"pg\\\";import{S3Client,ListObjectsV2Command}from\\\"@aws-sdk/client-s3\\\";const h=x=>crypto.createHash(\\\"sha256\\\").update(x).digest(\\\"hex\\\");const pool=new Pool({connectionString:process.env.DATABASE_URL,max:1});const q=await pool.query(\\\"SELECT id,images FROM products ORDER BY id\\\");const rows=q.rows.map(r=>({id:r.id,images:r.images}));const refs=[];let bad=0;for(const r of rows){try{const a=typeof r.images===\\\"string\\\"?JSON.parse(r.images):r.images;if(Array.isArray(a))for(const k of a)if(typeof k===\\\"string\\\")refs.push(k);else bad++}catch{bad++}}const s3=new S3Client({endpoint:process.env.R2_ENDPOINT,region:process.env.R2_REGION||\\\"auto\\\",credentials:{accessKeyId:process.env.R2_ACCESS_KEY_ID,secretAccessKey:process.env.R2_SECRET_ACCESS_KEY},forcePathStyle:true});let token;const inv=[];do{const p=await s3.send(new ListObjectsV2Command({Bucket:process.env.R2_BUCKET,ContinuationToken:token}));for(const o of p.Contents||[])if(o.Key)inv.push({key:o.Key,size:o.Size||0,etag:o.ETag||null});token=p.IsTruncated?p.NextContinuationToken:undefined}while(token);inv.sort((a,b)=>a.key.localeCompare(b.key));const keys=new Set(inv.map(o=>o.key));const uniq=[...new Set(refs)].sort();const missing=uniq.filter(k=>!keys.has(k));const missingSide=uniq.filter(k=>!keys.has(k+\\\".evidence.json\\\"));const orphanPrimary=inv.filter(o=>!o.key.endsWith(\\\".evidence.json\\\")&&!uniq.includes(o.key)).map(o=>o.key);const orphanSide=inv.filter(o=>o.key.endsWith(\\\".evidence.json\\\")&&!uniq.includes(o.key.slice(0,-14))).map(o=>o.key);console.log(JSON.stringify({schema:\\\"score80-l-paired-inventory/v1\\\",observed_at:new Date().toISOString(),exact_sha:process.env.RENDER_GIT_COMMIT||null,database:{name:(await pool.query(\\\"SELECT current_database() n\\\")).rows[0].n,product_rows:rows.length,row_fingerprint:h(JSON.stringify(rows))},r2:{bucket_identity_sha256:h(process.env.R2_BUCKET),object_count:inv.length,inventory_fingerprint:h(JSON.stringify(inv))},correlation:{reference_entries:refs.length,unique_reference_keys:uniq.length,matched_primary:uniq.length-missing.length,missing_primary:missing.length,matched_sidecar:uniq.length-missingSide.length,missing_sidecar:missingSide.length,orphan_primary:orphanPrimary.length,orphan_sidecar:orphanSide.length,bad_images_columns:bad,missing_primary_key_hashes:missing.map(h),missing_sidecar_key_hashes:missingSide.map(h),orphan_primary_key_hashes:orphanPrimary.map(h),orphan_sidecar_key_hashes:orphanSide.map(h)},mutation:false}));await pool.end();\""
}
