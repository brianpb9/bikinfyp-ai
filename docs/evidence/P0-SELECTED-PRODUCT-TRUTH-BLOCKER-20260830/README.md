# Selected product truth blocker — 2026-08-30

Founder selected one product and exact truth targets, but the normal paths remain fail-closed for two independent authority reasons. The exact R2 bytes exist, yet authorization is UNCONFIRMED because the sidecar and durable rights receipt are absent. The product owner has no email for the normal OTP path. The guarded C5 role and principal bindings are both unset; no principal is inferred from activity.

No product truth, category, reference, candidate, provider, ledger, payment, output, publication, or production mutation occurred. `verify.mjs` cross-checks the non-secret receipts. Founder must select an exact C5 principal before any runtime binding, and a separate durable rights receipt is required before reference authorization can ever become YES.

