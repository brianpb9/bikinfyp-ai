# Candidate #4 creation, activation, and structured stale-sweep survival

The reviewed staging write created candidate #4 exactly once as job `2c49a5c8-9465-4400-a214-159336a2c097`. `POST-CREATION-RECEIPT.json` binds the atomic lifecycle receipt. `POST-EXIT-READBACK.json` is a fresh-process/fresh-pool readback. `INDEPENDENT-STATE-READBACK.json` is a later managed `REPEATABLE READ READ ONLY` query proving exactly two product jobs/scripts, one exact candidate #4, no candidate #5, the sole predecessor still `REFUNDED`, and zero provider/output/publication effects. `MANAGED-READBACK-JOB-RAW.json` and `MANAGED-READBACK-LOG-RAW.json` archive the exact Render API receipts. `SCRIPT-DIGEST-MANAGED-JOB-RAW.json` and `SCRIPT-DIGEST-MANAGED-LOG-RAW.json` additionally bind the R2 read-only script/manual-audit readback and exact executed source; the verifier independently recomputes the frozen script digest and matches migration 0046. The verifier binds every receipt by SHA-256 and cross-checks service/job identity, status/timestamps, resource label, runtime SHA, log message, and the base64 inline source actually executed.

The runner requires an explicit candidate-4 confirmation, locks the product and historical candidate, proves candidate #3 is the sole prior candidate and is `REFUNDED` with zero provider effects, proves no candidate-4 script/job/lifecycle history exists, and admits exactly one new job through the canonical HTTP approval and job routes. Any committed ambiguity is preserved; rerun cannot create candidate #5.

The metadata freeze/activation path accepts candidate #4 only when its exact job, correlation ID, lifecycle state digest, current database-binding digest, and frozen approved-script digest are supplied. `METADATA-PREFLIGHT-*` archives the exact reviewed-SHA preflight and `ACTIVATION-*` archives the single activation that inserted the authoritative `PREPOST_READY` row and six-hour `ACTIVE_EVIDENCE_LEASE`. Both receipts cross-check the predecessor's terminal ledger and zero-effect invariants with candidate #4's one hold and zero provider/output/publication effects.

`LEASE-PRE-SWEEP-*` proves the queued candidate was already older than the normal 30-minute stale threshold while protected by its unexpired lease. `WORKER-DEPLOY-RAW.json` and `WORKER-RUNTIME-LOGS-RAW.json` bind a canonical worker startup to reviewed SHA `13c22bc7a3a340f0ea5f4bb0db9a905691676c77`; `LEASE-POST-SWEEP-*` proves the exact candidate, evidence row, ledger, and zero-effect counts were unchanged later, and `WORKER-POST-SWEEP-SERVICE-RAW.json` proves the worker was suspended again.

`LEASE-FRESH-PRE-SWEEP-MANAGED-{JOB,LOG}-RAW.json` records a new managed readback immediately before the final sweep gate. `WORKER-STRUCTURED-SWEEP-DEPLOY-RAW.json` binds the production worker deployment to reviewed SHA `20e505c38a5320ebe33c82a1c0c84c990dd975ca`, and `WORKER-STRUCTURED-SWEEP-LOG-RAW.json` archives the worker's explicit `POSTGRES_STALE_SWEEP_COMPLETED` receipt with its runtime SHA, interval trigger, start/completion timestamps, and zero reconciliation counts. `LEASE-FRESH-POST-SWEEP-MANAGED-{JOB,LOG}-RAW.json` proves the exact candidate, evidence lease, ledger, paused queue, and zero provider/output/publication effects remained unchanged after that receipt. `WORKER-STRUCTURED-SWEEP-POST-SUSPEND-SERVICE-RAW.json` proves the worker was suspended again after the post-sweep readback. The verifier hashes every fresh receipt and enforces the ordering pre-readback < sweep receipt < post-readback < suspension, so the packet now proves an actual exact-SHA production stale sweep rather than inferring one from elapsed time.
